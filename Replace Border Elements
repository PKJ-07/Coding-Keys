/*
Problem Statement: 
The program must accept an integer matrix of size RxC as the input. The program must replace all the border elements with * (asterisk) and then the program must print the modified matrix as the output.

Boundary Condition(s):
2 <= R, C <= 50

Input Format:
The first line contain R and C separated by a space.
The next R lines each contain C integers separated by space(s).

Output Format: 
The first R lines each contain C elements of the modified matrix separated by a space.

Example Input/Output 1: 
Input: 
4 5
1 1 2 5 3
9 3 4 9 8
2 8 8 5 6
6 8 2 4 2

Output: 
* * * * *
* 3 4 9 *
* 8 8 5 *
* * * * *

Explanation:
All the border elements are replaced by * (asterisk) and then the modified matrix is printed as the output.

Example Input/Output 2:
Input:
6 4
1 5 7 4
1 8 4 5
8 6 2 9
3 1 7 4
3 1 2 5
2 5 1 3

Output:
* * * *
* 8 4 *
* 6 2 *
* 1 7 *
* 1 2 *
* * * *

Note: Max Execution Time Limit: 4000 millisecs
*/
//SOLUTION:
#include <stdio.h>
#include <stdlib.h>
int main()
{
    int r,c;scanf("%d%d",&r,&c);
    int a[r][c];
    for(int i=0;i<r;i++){
        for(int j=0;j<c;j++){
            int s;scanf("%d",&s);
            if(i==0||i==r-1||j==0||j==c-1){
                printf("* ");
            }else{
                printf("%d ",s);
            }
        }
        printf("\n");
    }
}
 
